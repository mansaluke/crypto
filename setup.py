from setuptools import setup

setup(
    name='crypto',
    version='0.0.1',
    author='',
    packages=['crypto'],
    package_dir={'':'src'},
    description='',
    long_description=open('README.md').read(),
    install_requires=[],
)